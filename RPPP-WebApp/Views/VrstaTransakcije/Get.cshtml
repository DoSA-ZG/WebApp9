@model RPPP_WebApp.Models.VrstaTransakcije


<tr hx-target="this" hx-swap="outerHTML">
    <td class="text-left">@Model.Naziv</td>
    <td>
        <a asp-action="Edit"
           asp-route-id="@Model.IdvrstaTransakcije"
           class="btn btn-sm"
           title="Ažuriraj">
            <i class="fas fa-edit"></i>
        </a>
    </td>
    <td>
        <button class="btn-danger btn btn-sm" hx-confirm="Obrisati vrstu transakcije?" hx-delete="@Url.Action(nameof(VrstaTransakcijeController.Delete), new { id = Model.IdvrstaTransakcije })">
            <i class="fas fa-trash-alt"></i>
        </button>
    </td>

</tr>