@model RPPP_WebApp.Models.VrstaSuradnika


<tr hx-target="this" hx-swap="outerHTML">
    <td class="text-left">@Model.Naziv</td>
    <td class="text-left">@Model.Opis</td>
    <td>
        <a asp-action="Edit"
           asp-route-id="@Model.IdVrstaSuradnika"
           class="btn btn-sm"
           title="Ažuriraj">
            <i class="fas fa-edit"></i>
        </a>
    </td>
    <td>
        <button class="btn-danger btn btn-sm" hx-confirm="Obrisati vrstu suradnika?" hx-delete="@Url.Action(nameof(VrstaSuradnikaController.Delete), new { id = Model.IdVrstaSuradnika })">
            <i class="fas fa-trash-alt"></i>
        </button>
    </td>

</tr>