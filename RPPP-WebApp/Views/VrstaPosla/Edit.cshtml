@model RPPP_WebApp.Models.VrstaPosla

@{
    ViewBag.Title = "Ažuriranje vrste posla";
}

<form asp-route-id="@Model.IdVrstaPosla"
      asp-route-page="@ViewBag.Page"
      asp-route-sort="@ViewBag.Sort"
      asp-route-ascending="@ViewBag.Ascending"
      asp-action="Edit"
      method="post">

    <td class="text-start">
        <span asp-validation-for="IdVrstaPosla" class="text-danger"></span>
        <input asp-for="IdVrstaPosla" class="form-control" type="hidden"/>
    </td>

    <td class="text-start">
        <label asp-for="Naziv"></label>
        <span asp-validation-for="Naziv" class="text-danger"></span>
        <input asp-for="Naziv" class="form-control" />
    </td>

    <div class="text-center">
        <button class="btn btn-primary" type="submit">Ažuriraj</button>
        <a asp-action="Index"
           asp-route-page="@ViewBag.Page"
           asp-route-sort="@ViewBag.Sort"
           asp-route-ascending="@ViewBag.Ascending"
           class="btn btn-secondary">
            Odustani
        </a>
    </div>

</form>